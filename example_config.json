{
    "log_endpoint_url": true,
    "endpoints": [
        {
            "endpoint": "/api/v1/test",
            "allowed_headers": [
                "Authorization",
                "Content-Type"
            ]
        }
    ]
}
